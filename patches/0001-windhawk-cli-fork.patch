diff --git a/src/windhawk/app/app.cpp b/src/windhawk/app/app.cpp
index 3941a75..f30fed2 100644
--- a/src/windhawk/app/app.cpp
+++ b/src/windhawk/app/app.cpp
@@ -281,7 +281,7 @@ void RunDaemon() {
     }
 
     wil::unique_mutex_nothrow mutex(
-        ::CreateMutex(nullptr, TRUE, L"WindhawkDaemon"));
+        ::CreateMutex(nullptr, TRUE, L"WindhawkCLIDaemon"));
     THROW_LAST_ERROR_IF_NULL(mutex);
 
     if (GetLastError() == ERROR_ALREADY_EXISTS) {
@@ -314,6 +314,17 @@ void RunDaemon() {
     CMessageLoopAlwaysRunOnIdle loop;
     _Module.AddMessageLoop(&loop);
 
+    // AutomaticUpdates: for the portable version this process hosts the engine, so
+    // update mods now, synchronously, before the engine starts and injects anything.
+    if (portable &&
+        StorageManager::GetInstance()
+            .GetAppConfig(L"Settings", false)
+            ->GetInt(L"AutomaticUpdates")
+            .value_or(0)) {
+        LOG(L"AutomaticUpdates enabled: updating mods before engine start");
+        Functions::RunModUpdater(/*wait=*/true, /*noReload=*/true);
+    }
+
     CMainWindow wnd(trayOnly, portable);
     wnd.Create(nullptr);
     // wnd.ShowWindow(SW_SHOW);
@@ -338,17 +349,17 @@ void CheckForUpdates() {
 
 void NotifyNewUpdatesFound() {
     SetNamedEventForAllSessions(
-        L"Global\\WindhawkNewUpdatesFoundEvent-daemon-session=");
+        L"Global\\WindhawkCLINewUpdatesFoundEvent-daemon-session=");
 }
 
 void NotifyAppSettingsChanged() {
     if (StorageManager::GetInstance().IsPortable()) {
-        SetNamedEvent(L"WindhawkAppSettingsChangedEvent-daemon");
+        SetNamedEvent(L"WindhawkCLIAppSettingsChangedEvent-daemon");
         return;
     }
 
     SetNamedEventForAllSessions(
-        L"Global\\WindhawkAppSettingsChangedEvent-daemon-session=");
+        L"Global\\WindhawkCLIAppSettingsChangedEvent-daemon-session=");
 }
 
 void ExitApp(bool wait, DWORD timeout) {
@@ -566,7 +577,7 @@ bool RunAsAdmin(PCWSTR parameters) {
 
 bool PostCommandToPortableRunningDaemon(
     CMainWindow::PortableAppCommand command) {
-    CWindow hDaemonWnd(FindWindow(L"WindhawkDaemon", nullptr));
+    CWindow hDaemonWnd(FindWindow(L"WindhawkCLIDaemon", nullptr));
     if (!hDaemonWnd) {
         return false;
     }
diff --git a/src/windhawk/app/functions.cpp b/src/windhawk/app/functions.cpp
index 8de915c..8c2061a 100644
--- a/src/windhawk/app/functions.cpp
+++ b/src/windhawk/app/functions.cpp
@@ -2,6 +2,8 @@
 
 #include "functions.h"
 
+#include "logger.h"
+
 namespace Functions {
 
 namespace {
@@ -615,4 +617,60 @@ void ApplyDialogLayoutRtl(CWindow wnd, bool isLayoutRtl) {
     wnd.InvalidateRect(NULL);
 }
 
+void RunModUpdater(bool wait, bool noReload) {
+    // Locate whcli.exe next to this executable; its directory is the install root
+    // (contains windhawk.ini) that whcli operates on.
+    WCHAR exePath[MAX_PATH];
+    DWORD len = GetModuleFileName(nullptr, exePath, ARRAYSIZE(exePath));
+    if (len == 0 || len >= ARRAYSIZE(exePath)) {
+        LOG(L"RunModUpdater: GetModuleFileName failed (%u)", GetLastError());
+        return;
+    }
+
+    std::wstring dir(exePath, len);
+    size_t slash = dir.find_last_of(L'\\');
+    if (slash == std::wstring::npos) {
+        LOG(L"RunModUpdater: unexpected exe path");
+        return;
+    }
+    dir.resize(slash);
+
+    std::wstring whcli = dir + L"\\whcli.exe";
+    if (GetFileAttributes(whcli.c_str()) == INVALID_FILE_ATTRIBUTES) {
+        LOG(L"RunModUpdater: whcli.exe not found at %s, skipping", whcli.c_str());
+        return;
+    }
+
+    // "auto-update" = self-update the app first (if a newer signed release exists),
+    // otherwise update mods. --no-reload is passed through to the mod-update step.
+    std::wstring cmdLine = L"\"" + whcli + L"\" auto-update --root \"" + dir + L"\"";
+    if (noReload) {
+        cmdLine += L" --no-reload";
+    }
+
+    STARTUPINFO si{};
+    si.cb = sizeof(si);
+    PROCESS_INFORMATION pi{};
+
+    // CreateProcess may write to the command-line buffer; pass a mutable copy.
+    std::wstring cmdLineMutable = cmdLine;
+    if (!CreateProcess(whcli.c_str(), cmdLineMutable.data(), nullptr, nullptr,
+                       FALSE, CREATE_NO_WINDOW, nullptr, dir.c_str(), &si, &pi)) {
+        LOG(L"RunModUpdater: CreateProcess failed (%u)", GetLastError());
+        return;
+    }
+
+    CloseHandle(pi.hThread);
+
+    if (wait) {
+        // Bound the pre-engine wait so a slow/hung update can't block startup forever.
+        constexpr DWORD kUpdaterTimeout = 1000 * 60 * 5;  // 5 min
+        if (WaitForSingleObject(pi.hProcess, kUpdaterTimeout) == WAIT_TIMEOUT) {
+            LOG(L"RunModUpdater: updater timed out, continuing startup");
+        }
+    }
+
+    CloseHandle(pi.hProcess);
+}
+
 }  // namespace Functions
diff --git a/src/windhawk/app/functions.h b/src/windhawk/app/functions.h
index f0f9ffb..a28188f 100644
--- a/src/windhawk/app/functions.h
+++ b/src/windhawk/app/functions.h
@@ -36,4 +36,10 @@ NTSTATUS CreateExecutionRequiredRequest(_In_ HANDLE ProcessHandle,
 bool IsRightToLeftLanguage(LANGID langId);
 void ApplyDialogLayoutRtl(CWindow wnd, bool isLayoutRtl);
 
+// Runs the bundled mod updater (whcli.exe update) for the current install.
+// noReload skips the disable/enable reload dance (use before the engine starts,
+// when there's nothing loaded to reload). If wait is true, blocks until the
+// updater exits or a timeout elapses.
+void RunModUpdater(bool wait, bool noReload);
+
 }  // namespace Functions
diff --git a/src/windhawk/app/main_window.cpp b/src/windhawk/app/main_window.cpp
index 8aa2a60..5a31be9 100644
--- a/src/windhawk/app/main_window.cpp
+++ b/src/windhawk/app/main_window.cpp
@@ -1,5 +1,7 @@
 #include "stdafx.h"
 
+#include <random>
+
 #include "main_window.h"
 
 #include "functions.h"
@@ -15,6 +17,16 @@ constexpr auto kUpdateInitialDelay = 1000 * 10;        // 10sec
 constexpr auto kUpdateInterval = 1000 * 60 * 60 * 24;  // 24h
 constexpr auto kUpdateRetryTime = 1000 * 60 * 60;      // 1h
 constexpr auto kModTasksDlgInitialDelay = 1000;        // 1sec
+constexpr UINT kAutomaticUpdateMinDelay = 1000u * 60 * 60 * 23;  // 23h
+constexpr UINT kAutomaticUpdateMaxDelay = 1000u * 60 * 60 * 24;  // 24h
+
+// Random interval in [23h, 24h] for the periodic AutomaticUpdates check.
+UINT GetRandomAutomaticUpdateDelay() {
+    static std::mt19937 rng(std::random_device{}());
+    std::uniform_int_distribution<UINT> dist(kAutomaticUpdateMinDelay,
+                                             kAutomaticUpdateMaxDelay);
+    return dist(rng);
+}
 
 ULONGLONG GetTaskbarProcessCreationTime() {
     HWND currentTaskbarWindow = FindWindow(L"Shell_TrayWnd", nullptr);
@@ -296,6 +308,16 @@ void CMainWindow::OnTimer(UINT_PTR nIDEvent) {
             SetTimer(Timer::kHandleNewProcesses, kHandleNewProcessInterval);
             break;
 
+        case Timer::kAutomaticUpdate:
+            KillTimer(Timer::kAutomaticUpdate);
+            LOG(L"AutomaticUpdates: periodic mod update");
+            // Run in the background: the engine (this same process, portable) reacts
+            // to config changes and whcli does the disable/enable reload dance per
+            // upgraded mod.
+            Functions::RunModUpdater(/*wait=*/false, /*noReload=*/false);
+            SetTimer(Timer::kAutomaticUpdate, GetRandomAutomaticUpdateDelay());
+            break;
+
         case Timer::kUpdateCheck:
             KillTimer(Timer::kUpdateCheck);
 
@@ -398,17 +420,10 @@ LRESULT CMainWindow::OnTrayIcon(UINT uMsg, WPARAM wParam, LPARAM lParam) {
             return Action::kNone;
         }
 
-        menu.AppendMenu(MF_STRING, static_cast<UINT_PTR>(Action::kOpenUI),
-                        Functions::LoadStrFromRsrc(IDS_TRAY_OPEN));
-        menu.AppendMenu(MF_SEPARATOR);
+        // windhawk-cli has no GUI: the tray shows the native loaded-mods status and exit.
         menu.AppendMenu(MF_STRING,
                         static_cast<UINT_PTR>(Action::kModTaskManager),
                         Functions::LoadStrFromRsrc(IDS_TRAY_LOADED_MODS));
-        menu.AppendMenu(
-            MF_STRING, static_cast<UINT_PTR>(Action::kToolkit),
-            (std::wstring(Functions::LoadStrFromRsrc(IDS_TRAY_TOOLKIT)) +
-             (m_disableToolkitHotkey ? L"" : L"\tCtrl+Win+W"))
-                .c_str());
         menu.AppendMenu(MF_SEPARATOR);
         menu.AppendMenu(MF_STRING, static_cast<UINT_PTR>(Action::kExit),
                         Functions::LoadStrFromRsrc(IDS_TRAY_EXIT));
@@ -426,14 +441,14 @@ LRESULT CMainWindow::OnTrayIcon(UINT uMsg, WPARAM wParam, LPARAM lParam) {
 
     switch (m_trayIcon->HandleMsg(wParam, lParam)) {
         case AppTrayIcon::TrayAction::kDefault:
-            action = Action::kOpenUI;
+            action = Action::kModTaskManager;  // no GUI: show native status instead
             break;
 
         case AppTrayIcon::TrayAction::kBalloon:
             if (m_lastUpdateStatus && m_lastUpdateStatus->appUpdateAvailable) {
                 action = Action::kOpenUpdatePage;
             } else {
-                action = Action::kOpenUI;
+                action = Action::kModTaskManager;  // no GUI: show native status instead
             }
             break;
 
@@ -539,8 +554,13 @@ void CMainWindow::InitForPortableVersion() {
 
     SetTimer(Timer::kHandleNewProcesses, kHandleNewProcessInterval);
 
+    if (settings->GetInt(L"AutomaticUpdates").value_or(0)) {
+        m_automaticUpdates = true;
+        SetTimer(Timer::kAutomaticUpdate, GetRandomAutomaticUpdateDelay());
+    }
+
     m_appSettingsChangedEvent.reset(Functions::CreateEventForMediumIntegrity(
-        L"WindhawkAppSettingsChangedEvent-daemon"));
+        L"WindhawkCLIAppSettingsChangedEvent-daemon"));
 
     FILETIME creationTime;
     FILETIME exitTime;
@@ -568,14 +588,14 @@ void CMainWindow::InitForNonPortableVersion() {
         ProcessIdToSessionId(GetCurrentProcessId(), &sessionId));
 
     std::wstring appSettingsChangedEventName =
-        L"Global\\WindhawkAppSettingsChangedEvent-daemon-session=" +
+        L"Global\\WindhawkCLIAppSettingsChangedEvent-daemon-session=" +
         std::to_wstring(sessionId);
 
     m_appSettingsChangedEvent.reset(Functions::CreateEventForMediumIntegrity(
         appSettingsChangedEventName.c_str()));
 
     std::wstring newUpdatesFoundEventName =
-        L"Global\\WindhawkNewUpdatesFoundEvent-daemon-session=" +
+        L"Global\\WindhawkCLINewUpdatesFoundEvent-daemon-session=" +
         std::to_wstring(sessionId);
 
     m_newUpdatesFoundEvent.reset(Functions::CreateEventForMediumIntegrity(
@@ -1028,7 +1048,7 @@ void CMainWindow::ShowLoadedModsDialog() {
         .dataSource = CTaskManagerDlg::DataSource::kModStatus,
         .sessionManagerProcessId = m_serviceInfo.processId,
         .sessionManagerProcessCreationTime = m_serviceInfo.processCreationTime,
-        .runButtonCallback = [this](HWND hWnd) { RunUI(hWnd); },
+        // No GUI to open in windhawk-cli; omit the run button (it would do nothing).
         .finalMessageCallback =
             [this](HWND hWnd) {
                 m_modStatusesDlg.reset();
diff --git a/src/windhawk/app/main_window.h b/src/windhawk/app/main_window.h
index a62cec7..b6727e7 100644
--- a/src/windhawk/app/main_window.h
+++ b/src/windhawk/app/main_window.h
@@ -14,7 +14,7 @@ class CMainWindow : public CWindowImpl<CMainWindow, CWindow, CNullTraits>,
                     public CMessageFilter,
                     public CIdleHandler {
    public:
-    DECLARE_WND_CLASS(L"WindhawkDaemon")
+    DECLARE_WND_CLASS(L"WindhawkCLIDaemon")
 
     // Custom messages.
     enum {
@@ -35,6 +35,7 @@ class CMainWindow : public CWindowImpl<CMainWindow, CWindow, CNullTraits>,
         kHandleNewProcesses = 1,
         kUpdateCheck,
         kModTasksDlgCreate,
+        kAutomaticUpdate,
     };
 
     enum class Hotkey {
@@ -112,6 +113,7 @@ class CMainWindow : public CWindowImpl<CMainWindow, CWindow, CNullTraits>,
     bool m_checkForUpdates = false;  // portable version only
     bool m_dontAutoShowToolkit = true;
     bool m_disableToolkitHotkey = false;
+    bool m_automaticUpdates = false;
     int m_modTasksDlgDelay = CTaskManagerDlg::kAutonomousModeShowDelayDefault;
 
     // Shown automatically when mods are doing tasks such as initializing or
diff --git a/src/windhawk/app/service.cpp b/src/windhawk/app/service.cpp
index 5d0e2d4..c154e76 100644
--- a/src/windhawk/app/service.cpp
+++ b/src/windhawk/app/service.cpp
@@ -245,6 +245,14 @@ VOID ServiceInstance::SvcInit(DWORD dwArgc, LPTSTR* lpszArgv) {
         StorageManager::GetInstance().GetAppConfig(L"Settings", false);
 
     if (!settings->GetInt(L"SafeMode").value_or(0)) {
+        // AutomaticUpdates: update mods before the engine starts/injects. Tell the
+        // SCM we need extra time so a slow update doesn't trip the start timeout.
+        if (settings->GetInt(L"AutomaticUpdates").value_or(0)) {
+            LOG(L"AutomaticUpdates: updating mods before engine start (service)");
+            ReportSvcStatus(SERVICE_START_PENDING, NO_ERROR, 6 * 60 * 1000);
+            Functions::RunModUpdater(/*wait=*/true, /*noReload=*/true);
+        }
+
         m_engineControl.emplace();
         m_engineControl->HandleNewProcesses();
     }
diff --git a/src/windhawk/app/service_common.h b/src/windhawk/app/service_common.h
index 528baff..1056a2e 100644
--- a/src/windhawk/app/service_common.h
+++ b/src/windhawk/app/service_common.h
@@ -2,21 +2,23 @@
 
 namespace ServiceCommon {
 
-static inline constexpr WCHAR kName[] = L"Windhawk";
+// Fork identity (WindhawkCLI) — distinct from the upstream "Windhawk" so the
+// service and its kernel objects don't collide with an official install.
+static inline constexpr WCHAR kName[] = L"WindhawkCLI";
 
 static inline constexpr WCHAR kInfoFileMappingName[] =
-    L"Global\\WindhawkServiceInfoFileMapping";
+    L"Global\\WindhawkCLIServiceInfoFileMapping";
 
-static inline constexpr WCHAR kMutexName[] = L"Global\\WindhawkServiceMutex";
+static inline constexpr WCHAR kMutexName[] = L"Global\\WindhawkCLIServiceMutex";
 
 static inline constexpr WCHAR kScanForProcessesEventName[] =
-    L"Global\\WindhawkScanForProcesses";
+    L"Global\\WindhawkCLIScanForProcesses";
 
 static inline constexpr WCHAR kEmergencyStopEventName[] =
-    L"Global\\WindhawkServiceEmergencyStopEvent";
+    L"Global\\WindhawkCLIServiceEmergencyStopEvent";
 
 static inline constexpr WCHAR kSafeModeStopEventName[] =
-    L"Global\\WindhawkServiceSafeModeStopEvent";
+    L"Global\\WindhawkCLIServiceSafeModeStopEvent";
 
 struct ServiceInfo {
     DWORD version;
diff --git a/src/windhawk/app/task_manager_dlg.cpp b/src/windhawk/app/task_manager_dlg.cpp
index f837a9c..dc8695b 100644
--- a/src/windhawk/app/task_manager_dlg.cpp
+++ b/src/windhawk/app/task_manager_dlg.cpp
@@ -168,8 +168,14 @@ void CTaskManagerDlg::LoadLanguageStrings() {
     }
     SetWindowText(title);
 
-    SetDlgItemText(IDOK,
-                   Functions::LoadStrFromRsrc(IDS_TASKDLG_BUTTON_OPEN_APP));
+    // windhawk-cli has no GUI to open: hide the "Open Windhawk" button when there's
+    // no run callback (otherwise the button would do nothing when clicked).
+    if (m_dialogOptions.runButtonCallback) {
+        SetDlgItemText(IDOK,
+                       Functions::LoadStrFromRsrc(IDS_TASKDLG_BUTTON_OPEN_APP));
+    } else if (CWindow okButton = GetDlgItem(IDOK)) {
+        okButton.ShowWindow(SW_HIDE);
+    }
 
     UINT columnStringIds[] = {
         IDS_TASKDLG_COLUMN_MOD,
